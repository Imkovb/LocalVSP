# Ubuntu Deployment Notes

Keep host-specific access details out of git. Store them in a password manager or pass them to the deployment scripts at runtime.

Suggested private notes:

- Host/IP: <fill locally>
- SSH user: <fill locally>
- SSH authentication: key or password
- Remote install dir: /opt/localvsp
- Docker data dir: /opt/localvsp/data

